name: Deploy VulnBank

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to Server

    environment:
      name: production
      url: https://vulnbank.org

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Sync code to server
        run: |
          rsync -avz --delete \
            --exclude '.env' \
            -e "ssh -o StrictHostKeyChecking=no" \
            ./ \
            ${{ secrets.USERNAME }}@${{ secrets.HOST }}:/home/${{ secrets.USERNAME }}/vuln-bank/

      - name: Deploy on server
        run: |
          ssh -o StrictHostKeyChecking=no \
            ${{ secrets.USERNAME }}@${{ secrets.HOST }} << 'EOF'
            set -e

            cd /home/${{ secrets.USERNAME }}/vuln-bank

            if [ ! -f .env ]; then
              cp .env.example .env
            fi

            docker compose down
            docker compose up -d --build
            docker image prune -f
            docker compose ps
          EOF

      - name: Health Check
        run: |
          sleep 20
          curl -f http://vulnbank.org || (echo "Health check failed" && exit 1)

